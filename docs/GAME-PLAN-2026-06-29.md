# LeadCurate Game Plan — 2026-06-29

> Concise. To the point. State of the build + everything still needed in priority order. Read CLAUDE.md §3 for pricing.

---

## ✅ What's actually live and working

| System | Status |
|---|---|
| Domain `leadcurate.com` | DNS swapped (propagating). HTTPS auto-issues 10–30 min after propagation. |
| Subdomain `n8n.leadcurate.com` | Live with valid Let's Encrypt cert |
| Business email `hello@leadcurate.com` | Live via Hostinger Agentic Mail |
| Inbound email pipeline | Live — every email to hello@ → Supabase `inbound_emails` → dashboard feed |
| Outbound email | Live — via `intake-autoresponse` Edge Function called from dashboard Send button |
| Intake form | Live at `/intake/` — 20 markets, 11 lanes, all fields |
| Supabase | 5 tables live: `intake_requests`, `prospects`, `inbound_emails`, `agent_tasks`, `private.app_secrets` |
| Dashboard `/command/` | Live — intake cards with [Review & Send Quote] + [Reject] buttons; conf:* chatter hidden |
| Customer auto-reply on click | Live — Codex closed the loop today (verified by test email "Targeted Premium") |
| Hermes (Danny) | On `google/gemini-2.5-flash` via OpenRouter; Sonnet 4.6 fallback; Codex/gpt-5.5 last resort |
| n8n workflow `intake_router` | Active — picks tier with 5-tier logic, writes back to row |
| 5-Tier pricing system | Locked in CLAUDE.md §3, dashboard, packages page, quote template |
| Auction scrapers (Mecklenburg/Fulton/Wake) | Weekly Sunday 2:15am cron — feeds Tier 3 Hot Sheet |

---

## 🎯 Open items — in priority order, with options

### P0 — Close architectural redundancy (free, ~30 min, Claude can do)
1. **Switch n8n `intake_router` from per-minute polling → Supabase webhook trigger.** Form submission instantly fires Hermes evaluation instead of waiting up to 60s.
2. **Disable Hermes conference-watcher cron.** It duplicates n8n's job for intakes. Hermes Telegram gateway stays so you can chat him directly.
3. **Remove FormSubmit from intake form HTML.** Dashboard + intake-autoresponse cover the same job better.

### P1 — Payment method (BLOCKER for first sale, only Derrick can decide)
**Recommendation:** start with two-in-one and migrate:
- **Phase 1 (today–first 3 sales):** **Cash App business tag** + **Zelle** — both free, instant, zero setup. Send instructions via Sent Quote email.
- **Phase 1.5 (after first 1 sale):** **Open Mercury Bank business account** (free, online application takes 1–3 days, no monthly fees, free ACH+wire receive). Use as the business bank account.
- **Phase 2 (after Mercury opens):** **Add Stripe** for card payments (2.9% + $0.30). Stripe deposits to Mercury. This is when invoicing looks pro.
- **Skip:** PayPal (looks unprofessional in B2B), Square (built for retail).

Action: pick which Cash App tag / Zelle email to use for Phase 1. I add them to the quote email template.

### P2 — Facebook Page + IG Business (the locked #1 channel)
You said you'd do this later. When ready:
1. Create FB Page from your personal account (Page name: LeadCurate)
2. Convert/create Instagram Business account, link via Meta Business Suite
3. developers.facebook.com → create App → generate Page Access Token with scopes `pages_show_list pages_read_engagement pages_manage_posts pages_messaging`
4. Paste token to me → I wire Danny's posting + DM monitoring

### P3 — Per-delivery customer audit report (ADDED TO ROADMAP per Derrick's request)
**Existing template:** `docs/system-audit/index.html` + `docs/property-numbers/index.html` are the design pattern (the 10-foot internal audit look).

**To build:** a generator that takes a customer's delivered list + market + lane mix and outputs a personalized HTML audit/strategy doc bundled with every Tier 2+ delivery. Contents:
- Lane breakdown ("Your 500 records: 312 tax-delinquent / 142 probate / 46 code violations")
- Per-lane working tips ("Probate: heirs respond fastest — call in first 7 days. Tax-delinquent: longest decision cycle — direct mail outperforms calls.")
- Median balance + equity stats for the file
- Suggested calling/mailing order
- Compliance note (one-liner re. TCPA on phones, public-record sourcing)

Tier eligibility: Bundle (Tier 4) + Exclusive Territory (Tier 5) come with the audit. Tier 1/2/3 don't, by default — this becomes a real upsell differentiator.

### P4 — X (your real-name account) + LinkedIn Company Page
- X: clean public-API token via developer.x.com → I wire Danny's posting same pattern as FB
- LinkedIn: Company Page from your personal profile + Pages API access. Lower priority than FB/IG.
- Skip TikTok, YouTube, Reddit for now.

### P5 — Phone (only if customers ask for it)
- Twilio number for SMS: $1/mo + per-message. Not worth setting up until 3+ customers ask.
- WhatsApp Business: free but requires Meta Business verification (1–3 days). Skip until volume warrants.

### P6 — Multi-lane scrape expansion (probate beyond 3 markets, code violations, etc.)
Currently 3 counties have auction data (Meck/Fulton/Wake). Probate is the highest-value lane per competitor data. To deliver probate widely we need scrapers for each county's probate court portal. This is a Codex/Danny build job once first sales prove demand for probate.

---

## 🛑 Things explicitly NOT doing right now

- ❌ Twilio / WhatsApp / phone numbers (defer until customer demand)
- ❌ Lead Scout cron (dormant by design)
- ❌ Landing page `/site/` polish (parked per THE-PLAN)
- ❌ Multi-tier subscription billing (Phase 3, after first 5 customers)
- ❌ Telegram alerter workflow (queued in agent_tasks; will revisit later)

---

## 🤔 Decisions only Derrick can make

1. **Payment method for first 3 sales** — Cash App tag? Zelle email? (Recommendation: BOTH)
2. **Open a Mercury Bank business account?** — Free, recommended path to Stripe later
3. **When do you want to do the Facebook Page setup?** — Today/this week/later
4. **P0 redundancy cleanup** — say "go" and I do it (zero cost, ~30 min, eliminates polling)

---

## 📋 Codex's remaining queue (after rate-limit clears)

Nothing critical. Optional polish:
- **Webhook conversion** for intake_router (P0.1 above — Claude can also do this)
- **Per-delivery audit report generator** (P3 above — bigger build, queue when first customer is in motion)
- **Send mail wrapper polish** (cleanup pass)

---

## 🤖 Hermes (Danny) — what he needs to know

The master file `/CLAUDE.md` is his source of truth. Updating it now with:
- 5-tier pricing system locked (§3) ✅ done earlier
- Brain on Gemini Flash + Sonnet fallback ✅ done earlier
- Conference Room watcher: still active but most agent chatter now skipped
- agent_tasks table: explicit task queue (vs activity_feed for logging)
- Intake auto-routing: he reads intake_requests, picks tier per §3, writes recommended_tier back

Danny does NOT need to do anything new right now. He's already operating per the updated rules.
