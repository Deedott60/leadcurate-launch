# Session Handoff — 2026-06-27 (evening)

> Resume point for the next Claude session. Master file `/CLAUDE.md` was updated this session — read it first.

## What this session did
- **n8n wired live.** Hostinger one-click up at `http://76.13.25.117:32768` (Derrick created the owner account). API key (JWT) stored on VPS at `/opt/leadcurate/.env` as `N8N_URL` + `N8N_API_KEY` (chmod 600). API verified HTTP 200. **No workflows built — wiring only**, per Derrick's "just connect it, don't build yet."
- **Dashboard agent dots fixed** — `docs/command/index.html`: removed phantom "Claude (orchestrator)" tile, dots now go green only on real activity_feed posts in the last 10 min. Commit `203bdf2`.
- **Built the ad/audit creative + LOCKED the design system** (see `/CLAUDE.md` §6): cream/navy/emerald only, NO red. Tier-explainer + "direction" ad sets, 4 sample county audits (Wake/Cobb/Fulton/Harris) with "SAMPLE" stamp + blurred addresses + High/Med/Low equity. Full recipe in user memory `feedback_leadcurate_ad_design.md`.
- **SSH restored** — `~/.ssh/config` had a UTF-8 BOM breaking all ssh from Derrick's Windows box. Stripped it (backup `config.bak.*`). `ssh leadcurate-vps` works again.
- **Cleaned prod data** — removed leftover RLS-test row from `customers` (`rls-test-anon-blocked@test.local`). Count now honestly 0.
- **Updated master `/CLAUDE.md`** + user memory (`leadcurate-vps`, new `leadcurate-ad-design`, `MEMORY.md` index).

## Verified true (via Supabase MCP + SSH, this session)
- **Codex Task 2 (Conference Room watcher): DONE** — conf:done 19:53, e2e-verified. Dashboard→Hermes execution channel is LIVE.
- Codex Tasks 1, 4, 5 also done (security 0 lints, intake autoresponse Edge Function deployed, tier plumbing).
- VPS crontab = exactly 2 jobs: `*/5` conference-watcher + `15 2 * * 0` auction scrapers.
- Data: intake_requests 0, prospects 0, scout_prospects 10 (all from manual test runs), customers 0.

## Correction to prior notes (verification discipline)
- **"Lead Scout running every 6 hours" was FALSE.** No cron, no systemd timer for it. The June 26–27 runs were manual tests. Scout is dormant and blocked on Reddit/BP credentials anyway. Corrected in `/CLAUDE.md` §13.

## Pending on Derrick (only he can do)
1. **Buy domain leadcurate.com + mailbox hello@leadcurate.com** (Hostinger) — decided, not yet purchased
2. **RESEND_API_KEY** (+ verified sender) → flips the intake autoresponder from ready to live
3. **Social credentials** — FB (burner/operator account, separate from personal brand-Page account), X
4. **Reddit/BiggerPockets credentials** (or session cookies) → before Lead Scout can harvest
5. **Hermes model** — considering DeepSeek V3 via OpenRouter (~$15)
6. First-customer outreach push (manual Phase 1)

## Next session: do this FIRST (the "scrapers tomorrow" reminder)
1. Read `/CLAUDE.md` (master) + this handoff.
2. Check `activity_feed` for anything new from Codex/Hermes since 2026-06-27 ~20:00.
3. **Scrapers / Lead Scout:** Derrick wants to start these tomorrow. They need (a) the cron installed AND (b) source credentials. Ask Derrick which sources he has creds for before installing any cron — don't schedule a scraper that will harvest 0.
4. Ask which blocker Derrick wants to clear first: domain, RESEND_API_KEY, or social/scraper creds.

## One-line status
Plumbing is done and verified; everything now waits on Derrick providing domain + credentials to flip "ready" into "live." Creative is built and the design system is locked.
